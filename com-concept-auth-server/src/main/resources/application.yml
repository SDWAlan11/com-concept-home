server:
  port: 8083

spring:
  application:
    name: com-concept-auth-server
  cloud:
    config:
      uri: http://localhost:8888

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO